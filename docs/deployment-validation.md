# Текущий проход

Изменения 2026-09-05 ещё не установлены; свежие результаты: [completion-pass.md](completion-pass.md). Данные ниже описывают предыдущую установку и не подтверждают текущую рабочую копию.

# Проверка и установка Godot-сборки

Дата: 2026-09-05. Статус: **installed; target MCP launch confirmed**.

Кандидат: `/private/tmp/iron-caravan-candidate-o7krc_f9`.
Целевой проект: `/Users/fosterushka/Work/vibe/roguelike_godot/new-game-project`.
Проверенная сборка установлена главным агентом в целевой проект. Запуск именно установленной версии через MCP подтверждён главным агентом: `run_project` и `get_debug_output` показали Metal / Apple M4 Pro и `OFFLINE_READY`.

## Проверки поведения

- Общий gate: **27/27 тестовых сценариев прошли** на стабильном снимке до двух последних исправлений. Runner проверял exit code, stdout и engine log, включая ошибки скрипта при exit0.
- Повторные целевые проверки кандидата после этих двух исправлений: **6/6 чисто**. Сценарии: presentation, session flow, integration, UI flow, controlled full run и visual smoke. Отчёт: `/private/tmp/iron-candidate-final-tests/report.json`.
- После этого исправлено перекрытие арсенала игровым баннером: отдельный UI-тест30/30 чисто, проверяет скрытие в арсенале/меню/паузе и сохранение таймера. Точные два файла перенесены в кандидат; финальный графический capture всех четырёх экранов чистый, главный агент просмотрел арсенал и подтвердил отсутствие перекрытия.
- Для mineHack получено60/60 presentation-проверок, включая точный исходный рецепт и выбор правильного аудиопотока.
- Controlled full run: шесть волн, пять компонентов босса, победа, контракт, запись профиля и рестарт с новым seed. Проверка использует управляемое уничтожение врагов; это не ручное прохождение и не подтверждение баланса.
- Combat soak:1800 симулированных секунд. Это отдельная проверка ограничений списков и состояния, а не1800 секунд графического benchmark.
- Полный забег человеком и прослушивание звука ещё не выполнены.

## Реальное изображение

Версия повторно проверена главным агентом через MCP: `4.7.2.stable.official.ed1daf0bf`.

Godot4.7.2, Metal / Forward+, Apple M4 Pro,1280×800. Главный агент просмотрел свежий ACES reference рядом с исходным Three.js reference с одинаковой камерой и стартовой комплектацией. Геометрия, дороги и композиция совпадают по этой проверке. Godot немного светлее, тени слабее. Это оставшееся отличие освещения; пиксельная идентичность не заявляется.

Reference снимает сцену без HUD, погоды и camera intro. Он не доказывает совпадение каждого эффекта или игрового сценария. Ошибок в свежем reference и графическом benchmark не зафиксировано. Отдельный графический world rebuild на Metal:33 проверки,0 ошибок (`/private/tmp/iron-candidate-gpu-rebuild.log`). Captures меню/игры/арсенала/loadout получены до исправления баннера; в арсенале обнаружено перекрытие и исправлено, финальный capture после правки чистый; меню, игра, арсенал и loadout сняты повторно, главный агент подтвердил исправление на изображении арсенала.

## Графический benchmark

[Исходные измерения JSON](validation/render-benchmark.json), fixture: `tests/render_benchmark.gd`.
Один запуск с уже импортированными ресурсами,180 кадров на фазу. Время кадра измерено между `frame_post_draw`; включает ожидание CPU/GPU. Это не независимая GPU timestamp-профилировка.

| Фаза | p50, мс | p95, мс | p99, мс | Максимум, мс |
| --- | ---: | ---: | ---: | ---: |
| Первые выстрелы | 8.316 | 9.345 | 33.004 | 139.839 |
| Первый показ поселения | 8.317 | 8.710 | 37.444 | 826.380 |
| Первое разрушение поселения | 8.328 | 8.541 | 10.490 | 24.210 |
| Шторм и молния | 8.378 | 9.143 | 9.934 | 10.966 |
| 106 акторов | 7.519 | 14.008 | 14.767 | 14.865 |

**Задержки первого использования не устранены.** Несмотря на p95 около8.5–14мс, первый показ поселения дал кадр826.380мс, а фаза первых выстрелов139.839мс. Нельзя заявлять плавную загрузку или отсутствие первых зависаний. Причина этих выбросов ещё не установлена; прогрев сам по себе не доказывает готовность всех сценовых путей.

- Загрузка меню с импортированным кешем:5.016с. Готовность первого заезда:5.517с, включая исходный countdown2.65с.
- Первое событие выстрела после тестового спавна:16.710мс. Это задержка события, отдельная от максимального времени кадра139.839мс.
- Три повторных заезда по seed0,991827,72841:5.191/4.450/4.671с до готовности, включая countdown.
- Во время пяти фаз: до510 draw calls,106 акторов,10757 узлов; native static allocator около299–300МБ, renderer video memory659МБ.
- После трёх перестроений: runtime nodes3, resources165; static allocator306.137/304.970/306.417МБ, video memory около664МБ. Разное число world nodes соответствует разным мирам. Три перестроения не доказывают отсутствие утечки в длительной игре.
- Native static allocator не является памятью процесса RSS. Показатели CPU process в JSON сохраняются как диагностические счётчики движка и не используются здесь для точной причинной атрибуции выбросов между фазами.

## Установка и экспорт

Главный агент выполнил безопасный перенос с проверкой SHA-256 по `baseline-files.json`:522 файла,483 созданы,15 заменены,24 совпадали,0 конфликтов. Файлы не удалялись. Резервная копия перезаписанных файлов: `/private/tmp/iron-caravan-deploy-backup-0winvbg8`. Запуск установленной версии подтверждён отдельными MCP `run_project` и `get_debug_output`: Metal / Apple M4 Pro, `OFFLINE_READY` для мира, машины, оружия, врагов, эффектов и HUD. Runtime `ERROR` и `SCRIPT ERROR` не обнаружены. В debug output остаются предупреждения о перекрытии имён, неиспользуемых значениях и целочисленном делении; запуск не является warning-free.

macOS export preset подготовлен, JSON включены. Export templates отсутствуют; самостоятельный дистрибутив не экспортирован и запуск экспортной сборки без сети не проверен. Запуск проекта в установленном Godot не равен проверке экспортного пакета.

После импорта главный агент сравнил SHA-256519 файлов целевого проекта с кандидатом, исключая три обновляемых status-документа: совпадение полное. Проверка запуска до меню не заменяет ручное прохождение, прослушивание или экспортный запуск.
